[package]
name = "cardstock"
version = "0.1.0"
edition = "2021"

[dependencies]
laminar = "0.5.0"
eframe = "0.16.0"

serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
persistence = ["eframe/persistence", "serde"]
